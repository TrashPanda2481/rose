# Bugs — kernel core

One entry per bug. Keep closed ones, don't delete — they're history.

Format:

```
## [OPEN|FIXED] short title
Date found:
Date fixed:
Symptom:
Cause:
Fix:
Commit/ref:
```

No entries yet. Nothing built to break.
